//Media and antibiotics functions
//Date 2-20-2015

#include "functions.h"

double bloodAgar ()
{
	double price = 56.80;
	int noPlates = 100; 
	double pricePerPlate = price / noPlates; 
	return pricePerPlate;
}

double fungalPlate ()
{
	double price = 101.16;
	int noPlates = 100;
	double pricePerPlate = price / noPlates;
	return pricePerPlate;
}

double mannitolPlate ()
{
	double price = 41.7;
	int noPlates = 20;
	double ppp = price / noPlates;
	return ppp;
}

double macConkeyPlates ()
{
	double price = 12.16;
	int noPlates = 20;
	double ppp = price / noPlates; 
	return ppp;
}

double MHplates()
{
	double price = 65.61;
	double noPlates = 20;
	double ppp = price / noPlates;
	return ppp;
}

double sterileSwabs ()
{
	double price = 113.2;
	int noSwabs = 100; //per box
	int noBoxes = 10;
	double pps = price / (noSwabs * noBoxes);
	return pps;
}

double TSBmedia ()
{
	double price = 105.3;
	int noTubes = 100;
	double pptube = price / noTubes;
	return pptube;
}

double thioMedia ()
{
	double price = 63.95;
	int noTubes = 100;
	double pptube = price / noTubes;
	return pptube;
}

double ampicillin ()
{
	double price = 86.53;
	int noDisks = 500;
	double ppdisk = price / noDisks;
	return ppdisk;
}

double amoxClav ()
{
	double price = 86.52;
	int noDisks = 500;
	double ppdisk = price / noDisks;
	return ppdisk;
}

double cefazolin ()
{
	double price = 86.52;
	int noDisks = 500;
	double ppdisk = price / noDisks;
	return ppdisk;
}

double cefoxitin ()
{
	double price = 86.52;
	int noDisks = 500;
	double ppdisk = price / noDisks;
	return ppdisk;
}

double ceftriazone ()
{
	double price = 87.37;
	int noDisks = 500;
	double ppdisk = price / noDisks;
	return ppdisk;
}

double doxycycline ()
{
	double price = 86.64;
	int noDisks = 500;
	double ppdisk = price / noDisks;
	return ppdisk;
}

double enrofloxacin ()
{
	double price = 19.93;
	int noDisks = 5;
	double ppdisk = price / noDisks;
	return ppdisk;
}

double gentamicin ()
{
	double price = 86.52;
	int noDisks = 500;
	double ppdisk = price / noDisks;
	return ppdisk;
}

double marbofloxacin ()
{
	double price = 17.43;
	int noDisks = 5;
	double ppdisk = price / noDisks;
	return ppdisk;
}

double oxacillin ()
{
	double price = 87.36;
	int noDisks = 500;
	double ppdisk = price / noDisks;
	return ppdisk;
}

double TrimethSulfa ()
{
	double price = 86.52;
	int noDisks = 500;
	double ppdisk = price / noDisks;
	return ppdisk;
}

double vancomycin ()
{
	double price = 86.52;
	int noDisks = 500;
	double ppdisk = price / noDisks;
	return ppdisk;
}

